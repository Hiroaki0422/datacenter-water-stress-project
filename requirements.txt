# Water Stress Watch v0/v1 — Python dependencies
# Regenerate the venv with:
#   python3 -m venv .venv && .venv/bin/pip install -r requirements.txt

# Data processing
pandas==3.0.3
numpy==2.4.6

# Geocoding / spatial join
shapely==2.1.2
pgeocode==0.5.0

# HTTP for WRI pull
requests==2.34.2

# Map (Week 3) — Folium generates Leaflet HTML
folium==0.20.0
branca==0.8.1

# ML (Week 4) — v1 XGBoost training on Colab Pro
# Pin loosely; Colab may ship slightly different versions
xgboost>=2.0.0
scikit-learn>=1.4.0
optuna>=3.6.0          # hyperparameter sweep (Week 5+)
shap>=0.45.0           # model explainability
joblib>=1.3.0          # model artifact serialization
matplotlib>=3.8.0      # validation plots in the Colab notebook
