# venv and cache
.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Open-Meteo cache (regenerable, ~50 MB)
data/external/open_meteo/

# WRI raw cache (regenerable, ~5 MB)
data/external/wri_aqueduct/aqueduct40_us_province_bws.json

# ATLAS (All Rights Reserved — do NOT track)
data/raw/atlas_datacenters.csv
data/raw/atlas_datacenters.geojson
data/external/atlas_datacenters.csv
data/external/atlas_datacenters.geojson

# v1 model artifacts (regenerable from notebooks/04_ml_training.ipynb)
models/*.pkl
models/*.joblib
notebooks/.ipynb_checkpoints/
# Note: data/processed/v1_predicted_wue.csv and docs/v1_vs_v0_comparison.md
# are intentionally tracked. The CSV is the v1.0 headline output (308 KB);
# the MD is the journalism-derivative comparison report. Both are small
# enough to live in git.

# OS
.DS_Store
Thumbs.db
